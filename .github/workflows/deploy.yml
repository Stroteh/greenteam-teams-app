name: Deploy GreenTeam to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
    # 1. Checkout kod
    - name: Checkout repository
      uses: actions/checkout@v4
    
    # 2. Preveri da li postoje svi potrebni fajlovi
    - name: Check required files
      run: |
        echo "📁 Checking required files..."
        
        if [ ! -f "index.html" ]; then echo "❌ index.html not found"; exit 1; else echo "✅ index.html exists"; fi
        if [ ! -f "style.css" ]; then echo "❌ style.css not found"; exit 1; else echo "✅ style.css exists"; fi
        if [ ! -f "funkcije.js" ]; then echo "❌ funkcije.js not found"; exit 1; else echo "✅ funkcije.js exists"; fi
        if [ ! -f "teams-integration.js" ]; then echo "❌ teams-integration.js not found"; exit 1; else echo "✅ teams-integration.js exists"; fi
        if [ ! -f "firebase-service.js" ]; then echo "❌ firebase-service.js not found"; exit 1; else echo "✅ firebase-service.js exists"; fi
        if [ ! -f "firebase-config.js" ]; then echo "❌ firebase-config.js not found"; exit 1; else echo "✅ firebase-config.js exists"; fi
    
    # 3. Zamijeni placeholdere u firebase-config.js sa stvarnim vrijednostima 
    - name: Update Firebase config with secrets
      run: |
        echo "🔧 Updating firebase-config.js with secrets..."
        
        # Proveri da li fajl postoji
        if [ ! -f "firebase-config.js" ]; then
          echo "❌ firebase-config.js not found"
          exit 1
        fi
        
        # Pročitaj trenutni sadržaj
        echo "Current firebase-config.js (first 5 lines):"
        head -5 firebase-config.js
        
        # Zamijeni placeholdere sa stvarnim vrijednostima
        # Za macOS/Linux kompatibilnost
        sed -i.bak "s|_FIREBASE_API_KEY_|${{ secrets.FIREBASE_API_KEY }}|g" firebase-config.js && rm firebase-config.js.bak
        sed -i.bak "s|_FIREBASE_AUTH_DOMAIN_|${{ secrets.FIREBASE_AUTH_DOMAIN }}|g" firebase-config.js && rm firebase-config.js.bak
        sed -i.bak "s|_FIREBASE_PROJECT_ID_|${{ secrets.FIREBASE_PROJECT_ID }}|g" firebase-config.js && rm firebase-config.js.bak
        sed -i.bak "s|_FIREBASE_SENDER_ID_|${{ secrets.FIREBASE_SENDER_ID }}|g" firebase-config.js && rm firebase-config.js.bak
        sed -i.bak "s|_FIREBASE_APP_ID_|${{ secrets.FIREBASE_APP_ID }}|g" firebase-config.js && rm firebase-config.js.bak
        
        echo "✅ Firebase config updated with secrets"
        echo "Updated firebase-config.js (first 5 lines):"
        head -5 firebase-config.js
    
    # 4. Setup GitHub Pages
    - name: Setup Pages
      uses: actions/configure-pages@v3
    
    # 5. Upload artifact (ažurirano na v3)
    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: '.'
    
    # 6. Deploy to GitHub Pages
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
      with:
        timeout: 600000  # 10 minutes timeout
    
    # 7. Success message
    - name: Success
      run: |
        echo "🎉 Deployment successful!"
        echo "🌐 Your app is available at:"
        echo "https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}"
        echo ""
        echo "📋 Deployment details:"
        echo "Environment: ${{ vars.ENVIRONMENT_NAME || 'github-pages' }}"
        echo "URL: ${{ steps.deployment.outputs.page_url }}"
